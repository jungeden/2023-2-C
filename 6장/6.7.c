/* ----------------------------------- 6.7 ---------------------------------- */
/*사용자로부터 키를 입력받아서 표준 체중을 계산한 후에 사용자의 체중과 비교하여 저체중인지, 표준 인지, 과체중인지를 판단하는 프로그램을 작성하라. 
표준 체중 계산식은 다음을 사용하라.*/

#include <stdio.h> 

int main(void)
{
    int h,w;
    
    printf("키와 체중을 입력하시오:"); scanf("%d %d",&h,&w);
    double a=(h-100)*0.9;

    if(w>a) printf("과체중입니다.");
    else if(w==a) printf("표준체중입니다.");
    else if(w<a) printf("저체중입니다.");

    return 0;
}